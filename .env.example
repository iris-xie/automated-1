# --- Firecrawl settings ---
# Base URL of local Firecrawl v2 service
FIRECRAWL_BASE_URL=http://localhost:3002

# Preferred auth: set your token only; program auto-builds "Authorization: Bearer <token>"
FIRECRAWL_TOKEN=

# Legacy/compatible auth variable. If set and not starting with "Bearer ", it will be prefixed automatically.
FIRECRAWL_AUTH=

# Extra scrape options merged into request payload (JSON string)
# Example: {"crawlOptions": {"maxDepth": 2, "limit": 50}, "includeSelectors": ["article"], "removeSelectors": ["nav", "footer"]}
FIRECRAWL_EXTRA_SCRAPE_OPTIONS={"blockAds": true,"skipTlsVerification": true,"waitFor": 1000,"formats":["markdown"]}


# --- Ollama settings ---
# Base URL of local Ollama service
OLLAMA_BASE_URL=http://localhost:11434

# Model name for translation and extraction
OLLAMA_MODEL=qwen:3b


# --- Output ---
# Directory to write generated Markdown files and manifest.json
OUTPUT_DIR=results

